# Development Environment Configuration
# Frontend will run on port 4001, backend on port 9001

# API Base URL - used for direct API calls (not proxied)
# In development, we use Vite's proxy, so this points to localhost
VITE_API_BASE_URL=http://localhost:9001

# WebSocket Base URL - used for WebSocket connections
# This will be automatically proxied through Vite dev server
VITE_WS_BASE_URL=ws://localhost:9001

# Frontend development server port
VITE_PORT=4001

# Enable API request logging
VITE_API_LOGGING=true
